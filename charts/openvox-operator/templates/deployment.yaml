apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openvox-operator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openvox-operator.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "openvox-operator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "openvox-operator.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "openvox-operator.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          {{- if .Values.image.digest }}
          image: "{{ .Values.image.repository }}@{{ .Values.image.digest }}"
          {{- else }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          {{- end }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            {{- if .Values.leaderElect }}
            - --leader-elect
            {{- end }}
            {{- if eq .Values.scope.mode "namespace" }}
            - --watch-namespace={{ .Values.scope.watchNamespace | default .Release.Namespace }}
            {{- end }}
            {{- if .Values.webhook.enabled }}
            - --enable-webhooks
            {{- end }}
            {{- if not .Values.gatewayAPI.enabled }}
            - --enable-gateway-api=false
            {{- end }}
            {{- if .Values.metrics.enabled }}
            - --metrics-bind-address=:{{ .Values.metrics.port | default 8080 }}
            - --metrics-secure={{ .Values.metrics.secure }}
            {{- if and .Values.metrics.secure (or .Values.metrics.certManager.enabled .Values.metrics.tls.certSecret) }}
            - --metrics-cert-dir=/tmp/k8s-metrics-server/serving-certs
            {{- end }}
            {{- else }}
            - --metrics-bind-address=0
            {{- end }}
          ports:
            - name: health
              containerPort: 8081
              protocol: TCP
            {{- if .Values.metrics.enabled }}
            - name: metrics
              containerPort: {{ .Values.metrics.port | default 8080 }}
              protocol: TCP
            {{- end }}
            {{- if .Values.webhook.enabled }}
            - name: webhook
              containerPort: {{ .Values.webhook.port | default 9443 }}
              protocol: TCP
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- $metricsCerts := and .Values.metrics.enabled .Values.metrics.secure (or .Values.metrics.certManager.enabled .Values.metrics.tls.certSecret) }}
          {{- if or .Values.webhook.enabled $metricsCerts }}
          volumeMounts:
            {{- if .Values.webhook.enabled }}
            - name: webhook-certs
              mountPath: /tmp/k8s-webhook-server/serving-certs
              readOnly: true
            {{- end }}
            {{- if $metricsCerts }}
            - name: metrics-certs
              mountPath: /tmp/k8s-metrics-server/serving-certs
              readOnly: true
            {{- end }}
          {{- end }}
      {{- if or .Values.webhook.enabled $metricsCerts }}
      volumes:
        {{- if .Values.webhook.enabled }}
        - name: webhook-certs
          secret:
            secretName: {{ .Values.webhook.tls.certSecret | default (printf "%s-webhook-cert" (include "openvox-operator.fullname" .)) }}
        {{- end }}
        {{- if $metricsCerts }}
        - name: metrics-certs
          secret:
            secretName: {{ .Values.metrics.tls.certSecret | default (printf "%s-metrics-cert" (include "openvox-operator.fullname" .)) }}
        {{- end }}
      {{- end }}
      terminationGracePeriodSeconds: 10
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
