{{- range $entry := .Values.pools }}
---
apiVersion: openvox.voxpupuli.org/v1alpha1
kind: Pool
metadata:
  name: {{ include "openvox-stack.poolName" (dict "root" $ "entry" $entry) }}
spec:
  configRef: {{ include "openvox-stack.configName" $ }}
  selector:
    openvox.voxpupuli.org/server: {{ include "openvox-stack.serverName" (dict "root" $ "entry" (dict "name" $entry.serverRef)) }}
  service:
    type: {{ $entry.service.type | default "ClusterIP" }}
    port: {{ $entry.service.port | default 8140 }}
  {{- if and $entry.route $entry.route.enabled }}
  route:
    enabled: true
    hostname: {{ $entry.route.hostname }}
    gatewayRef:
      name: {{ $.Values.gateway.name }}
      {{- with $.Values.gateway.sectionName }}
      sectionName: {{ . }}
      {{- end }}
    {{- if $entry.route.injectDNSAltName }}
    injectDNSAltName: true
    {{- end }}
  {{- end }}
{{- end }}
