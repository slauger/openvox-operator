{{- range $entry := .Values.servers }}
---
apiVersion: openvox.voxpupuli.org/v1alpha1
kind: Server
metadata:
  name: {{ include "openvox-stack.serverName" (dict "root" $ "entry" $entry) }}
spec:
  configRef: {{ include "openvox-stack.configName" $ }}
  certificateRef: {{ include "openvox-stack.certName" (dict "root" $ "entry" $entry) }}
  {{- if hasKey $entry "ca" }}
  ca: {{ $entry.ca }}
  {{- end }}
  {{- if hasKey $entry "server" }}
  server: {{ $entry.server }}
  {{- end }}
  {{- if $entry.poolRefs }}
  poolRefs:
    {{- range $ref := $entry.poolRefs }}
    - {{ include "openvox-stack.poolName" (dict "root" $ "entry" (dict "name" $ref)) }}
    {{- end }}
  {{- end }}
  replicas: {{ $entry.replicas | default 1 }}
  {{- with $entry.resources }}
  resources:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.javaArgs }}
  javaArgs: {{ . | quote }}
  {{- end }}
  {{- with $entry.maxActiveInstances }}
  maxActiveInstances: {{ . }}
  {{- end }}
  {{- with $entry.image }}
  image:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.code }}
  {{- if kindIs "slice" . }}
  code:
    {{- toYaml . | nindent 4 }}
  {{- else if or .image .claimName }}
  code:
    - {{ if .image }}image: {{ .image }}{{ else }}claimName: {{ .claimName }}{{ end }}
      {{- with .imagePullPolicy }}
      imagePullPolicy: {{ . }}
      {{- end }}
      {{- with .imagePullSecret }}
      imagePullSecret: {{ . }}
      {{- end }}
      {{- with .environment }}
      environment: {{ . }}
      {{- end }}
      {{- with .mountPath }}
      mountPath: {{ . }}
      {{- end }}
  {{- end }}
  {{- end }}
  {{- with $entry.autoscaling }}
  autoscaling:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.pdb }}
  pdb:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.networkPolicy }}
  networkPolicy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.topologySpreadConstraints }}
  topologySpreadConstraints:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.affinity }}
  affinity:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.priorityClassName }}
  priorityClassName: {{ . }}
  {{- end }}
  {{- with $entry.extraEnv }}
  extraEnv:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.envFrom }}
  envFrom:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.extraVolumes }}
  extraVolumes:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $entry.extraVolumeMounts }}
  extraVolumeMounts:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
