kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# ImageVolume is default-enabled since Kubernetes 1.35.
# Only needed for kind clusters running Kubernetes < 1.35.
featureGates:
  ImageVolume: true
nodes:
  - role: control-plane
